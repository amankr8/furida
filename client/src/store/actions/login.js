

export const login = (loginData) => async (dispatch) => {
    try {
        
    } catch (error) {
        console.error(error)
    }
}

export const logout = () => async (dispatch) => {
    try {
        
    } catch (error) {
        console.error(error)
    }
}